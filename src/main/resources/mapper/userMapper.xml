<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.groupfour.foodbox.mapper.UserMapper">

    <!--회원 가입-->
    <insert id="userRegister">
        insert into user (user_no, user_name, user_id, user_pw, user_email, user_tel,
        user_gender, user_birthday, user_zipcode, user_zibunaddr, user_detailaddr)
        values(#{user_no}, #{user_name}, #{user_id}, #{user_pw}, #{user_email}, #{user_tel}, #{user_gender},
        #{user_birthday}, #{user_zipcode}, #{user_zibunaddr}, #{user_detailaddr})
    </insert>

    <!--회원 리스트-->
    <select id="userList">
        select user_no, user_name, user_id, user_pw, user_email, user_tel,
        user_gender, user_birthday, user_zipcode, user_zibunaddr, user_detailaddr
        from user
    </select>

    <!--회원 삭제-->
    <delete id="userDelete">
        delete from user
        where user_no = #{user_no}
    </delete>

    <!--회원 선택 삭제-->
    <delete id="usersDelete">
        delete from user
        <where>
            <foreach item="item" index="index" collection="chkList" open="user_id in ("
                     separator="," close=")">
                #{item}
            </foreach>
        </where>
    </delete>



</mapper>